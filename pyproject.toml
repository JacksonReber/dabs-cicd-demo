[project]
name = "dabs_cicd_demo"
version = "0.0.1"
description = "DABs CI/CD reference demo — shared helpers (local testing only)"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
